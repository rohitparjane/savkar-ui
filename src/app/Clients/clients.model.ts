export interface Client {
      clName: String;
	  clMobile: String;
	  clAmount:String;
	  date: Date;
	  user: String;
      isHovered:boolean;
      showDetails: boolean; 
      clAddress:String;
    // Add more properties as needed
  }
  